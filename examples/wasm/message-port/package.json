{
  "name": "mqtt5-message-port-example",
  "version": "0.9.0",
  "description": "MQTT MessagePort example using mqtt5 WASM client with Service Worker",
  "type": "module",
  "scripts": {
    "build": "cd ../../.. && wasm-pack build --target web --features wasm-client && cp -r pkg examples/wasm/message-port/",
    "serve": "python3 -m http.server 8000",
    "dev": "npm run build && npm run serve"
  }
}
